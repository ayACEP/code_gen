const React = require("react");

class SQL extends React.Component {
    constructor(props) {
        super(props);
    }
    render() {
        return React.createElement(
            "p",
            null,
            "sql"
        );
    }
}

module.exports = SQL;